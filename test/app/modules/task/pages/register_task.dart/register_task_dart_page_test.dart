import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('RegisterTaskPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(RegisterTaskPage(title: 'RegisterTask')));
    //  final titleFinder = find.text('RegisterTask');
    //  expect(titleFinder, findsOneWidget);
  });
}
